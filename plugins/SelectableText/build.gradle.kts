version = "0.1.2"
description =
    "Make every text selectable!"

aliucord {
    changelog.set(
        """
        feat: init
        """.trimIndent()
    )

    deploy.set(true)
}

// android {
//     sourceSets {
//         getByName("main") {
//             kotlin.srcDir("stubs")
//         }
//     }
// }
