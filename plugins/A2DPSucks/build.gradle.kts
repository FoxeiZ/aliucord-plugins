version = "0.01"
description =
    "Make every text selectable!"

aliucord {
    changelog.set(
        """
        feat: init
        """.trimIndent()
    )

    deploy.set(false)
}

// android {
//     sourceSets {
//         getByName("main") {
//             kotlin.srcDir("stubs")
//         }
//     }
// }
